apiVersion: v1
kind: Service
metadata:
  name: {{ include "caronte-tienda.fullname" . }}
  labels:
    {{- include "caronte-tienda.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "caronte-tienda.selectorLabels" . | nindent 4 }}
