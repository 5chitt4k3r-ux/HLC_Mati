services:
  servidor:
    build:
      context: ${CONTEXTO}
      dockerfile: ./dockerfile/sweb/nginx/dockerfile
    image: kuklusklan/nginx
    container_name: ${CONTENEDOR}
    env_file:
      - ./.env
    ports:
      - ${PORT_WEB}:80
    volumes:
      - ./logs:/root/logs
    networks: 
      netcarontewebnginx:
        ipv4_address: ${IP}

networks:
  netcaronte:
    ipam:
      driver: default
      config:
        - subnet: ${SUBNET}